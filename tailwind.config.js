module.exports = {
  content: [
    "./views/**/*.hbs", // Scans .hbs files in the views directory and its subdirectories
    "./public/**/*.css", // Scans .css files in the public directory and its subdirectories
    // Add more patterns as needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
